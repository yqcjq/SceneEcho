"""Agent package — AIGC generation (``aigc``) + NL editing (``nl_edit``).

The Phase 1B placeholder ``generate_broll`` / ``generate_sticker_image``
stubs that used to live here were promoted to real implementations in
``app.agent.aigc`` (Phase 5, ISS-028). Import them from there directly:
``from app.agent.aigc import generate_broll``.
"""

from __future__ import annotations
